# Python Port Scanner

A fast, multithreaded TCP port scanner written in Python.

## Features
- TCP connect scanning
- Multithreading with limits
- Custom port ranges
- Domain name resolution
- Clean CLI interface

## Usage
```bash
python3 scanner.py -t <target> -p <start-end>
